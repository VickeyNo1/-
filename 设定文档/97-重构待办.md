# 97-重构待办

本文件只管理当前未完成工作，不保存旧版设定。重构前内容由 Git 标签 `pre-worldview-reboot-20260815` 与提交历史保存；此后删除的设定（副本模式、驭灵、旧区名等）同样只通过提交历史与 `99` 变更记录追溯。

重构各专题设定已完成，现行设定以 `01` 至 `05` 为准；全部待确认与延后处理设定统一见 `99-交接与待处理文档.md`。

## 当前优先

1. 第一卷改造：已完成模块化拆分（总纲加 `模块01` 至 `模块08`）；事件一卡死收容终局方案已定，接下来完整设计事件二与事件三（规律与卡死条件）。
2. 确定第一卷核心人物姓名、背景、动机与关系细节。
3. 拆分章节数量、关键转折和章末钩子。

## 执行纪律

- 只设计当前剧情需要的内容，不补无用途百科。
- 已确认事实写入唯一权威文档，索引只保留摘要和链接。
- 第一卷卷级方向、核心人物和章节结构确认前不写正文。
- B级或C级改动须同步更新权威文档、`99` 和相关执行层。
- 每次批量修改后扫描旧术语、断链与失效因果。
